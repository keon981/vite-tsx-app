{
  "name": "create-keon-vite-app",
  "version": "0.0.10",
  "description": "Example starter template tool",
  "scripts": {
    "start": "node bin/index.js",
    "publish:local": "pnpm publish --no-git-checks --registry http://localhost:4873"
  },
  "publishConfig": {
    "git-checks": false
  },
  "bin": {
    "new-template": "./bin/index.js"
  },
  "keywords": [
    "template"
  ],
  "license": "MIT",
  "dependencies": {
    "cross-spawn": "^7.0.3"
  }
}